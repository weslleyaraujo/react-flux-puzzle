export default {
  onCountDownChange: () => {
    // should set game timer data
    // should emit change
  },

  onCountDownDone: () => {
    // should set game over
  },

  increaseTime: () => {
    // should increase time
  }
}
