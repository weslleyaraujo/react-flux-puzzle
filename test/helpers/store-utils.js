import { expect } from 'chai';

describe('Store utiles', () => {
  describe('getSchema', () => {
    it('should return the store schema', () => {
      expect(1).to.be.a('number');
    });
  });

});

